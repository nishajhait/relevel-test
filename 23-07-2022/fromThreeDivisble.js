function dividedRule(num) {
    var newNum = num.toString()
    console.log(typeof newNum)
    for (i = 0; i <= newNum.length - 1; i++) {
        newNum[i]
        var num=number(str)
    }
}
dividedRule(34544)